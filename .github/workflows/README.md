Github workflows are turned off as the remote boards/shields do not include dactyl_manuform.

This can usually be remedied by adding zephyr/module.yml, but I can't find a way to push the file as the zephyr directory is used to clone the original zephyr repo. Best solution is to also push the builds.